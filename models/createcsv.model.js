class createcsv {
    constructor(nombre, calificacion, comentario, fecha) {
        this.nombre = nombre;
        this.calificacion = calificacion;
        this.comentario = comentario;
        this.fecha = fecha
    }
}

module.exports = createcsv